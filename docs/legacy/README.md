# Legacy Documentation

This directory contains some LLM slop that documents the transition from the old standalone scripts (1.0) to the new unified CLI (2.0).

In rewriting the 1.0 scripts, it has become evident that it's important to document their (sometimes divergent) behavior. The documents contained herein outline which steps have been taken to improve consistency between commands while attempting to maintain backwards compatibility where possible in 2.0.
